# Server Configuration
PORT=3344
CORS_ORIGIN=http://localhost:5173

# Runtime configuration directory, relative to the repository root.
# API credentials and agent profiles belong in this directory, not in .env.
GAME_CONFIGS_DIR=configs

# Game Configuration
GAME_RECORDS_DIR=./data/records
