# specs 索引

`specs` 使用树形语义：
1. 越靠祖先越稳定（长期核心规范）。
2. 越靠叶子越具体（短期迭代规范）。

目录：
1. `backend/`：后端规范树（已启用）。
2. `frontend/`：前端规范树（已启用）。
3. `shared/`：跨端共享规范树（待补充）。
4. `archive/`：历史归档。

配套关系：
1. 同构执行树见 `docs/drivers/readme.md`。
2. API 协议见 `docs/apis/readme.md`。
