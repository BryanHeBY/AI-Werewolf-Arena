# drivers 索引

## 1. 当前代码详细文档

本目录存放“面向模块/边界”的驱动文档：

1. 项目总驱动：`project_driver.md`
2. 后端驱动：`backend_driver.md`
3. 前端驱动：`frontend_driver.md`

定位：

- 适合做“从哪里改、改哪些模块、依赖哪些规范”的入口。
- 与活动驱动互补：模块驱动回答“改哪里”，活动驱动回答“怎么推进”。

## 2. 未来目标 TODO

- [ ] 为每个 driver 增加“本周优先级”区块。
- [ ] 增加“驱动文档之间的差异说明”，避免职责重叠。
- [ ] 将 driver 与 `docs/codebase` 关键节点建立一键跳转清单。

## 3. 验收标准

- [ ] 三份 driver 文档职责清晰、无重复堆叠。
- [ ] 任意开发任务可先落到某一个 driver 再映射到 codebase 文件。
- [ ] 发生结构调整时，本索引与三份 driver 同步更新。
